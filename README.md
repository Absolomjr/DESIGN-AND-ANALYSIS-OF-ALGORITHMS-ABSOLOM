The code is where i compared the time taken for each algorithm i.e of merge sort, insertion sort and quick sort to run on a given dataset.
i also compared their given time complexities using a graph.
